import type { ThemeSectionProps } from "../../../types"; export const variants={default:({config}:ThemeSectionProps)=><section id="about" className="px-6 py-14">{config.about}</section>,alt:({config}:ThemeSectionProps)=><section id="about" className="border-l-2 border-cyan-400 px-6 py-14">{config.about}</section>}; export function About({variant="default",...props}:ThemeSectionProps&{variant?:string}){if(!props.config.about)return null;return (variants[variant as keyof typeof variants]||variants.default)(props);}
