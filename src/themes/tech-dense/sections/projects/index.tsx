import type { ThemeSectionProps } from "../../../types"; export const variants={default:({config}:ThemeSectionProps)=><section id="projects" className="px-6 py-14">{config.projects?.map(x=><article key={x.id||x.title}>{x.title}</article>)}</section>,alt:({config}:ThemeSectionProps)=><section id="projects" className="border-l-2 border-cyan-400 px-6 py-14">{config.projects?.map(x=><article key={x.id||x.title}>{x.title}</article>)}</section>}; export function Projects({variant="default",...props}:ThemeSectionProps&{variant?:string}){if(!props.config.projects?.length)return null;return (variants[variant as keyof typeof variants]||variants.default)(props);}
