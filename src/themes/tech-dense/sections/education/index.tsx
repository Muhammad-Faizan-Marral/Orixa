import type { ThemeSectionProps } from "../../../types"; export const variants={default:({config}:ThemeSectionProps)=><section id="education" className="px-6 py-14">{config.education?.map(x=><p key={x.id||x.institution}>{x.degree} · {x.institution}</p>)}</section>,alt:({config}:ThemeSectionProps)=><section id="education" className="border-l-2 border-cyan-400 px-6 py-14">{config.education?.map(x=><p key={x.id||x.institution}>{x.degree} · {x.institution}</p>)}</section>}; export function Education({variant="default",...props}:ThemeSectionProps&{variant?:string}){if(!props.config.education?.length)return null;return (variants[variant as keyof typeof variants]||variants.default)(props);}
