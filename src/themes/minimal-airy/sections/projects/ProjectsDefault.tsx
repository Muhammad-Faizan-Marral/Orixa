import type { ThemeSectionProps } from "../../../types";
export function ProjectsDefault({config}:ThemeSectionProps){if(!config.projects?.length)return null;return <section id="projects" className="mx-auto max-w-6xl px-6 py-20"><h2 className="text-3xl font-semibold">Selected work</h2><div className="mt-8 grid gap-5 md:grid-cols-2">{config.projects.map(project=><article key={project.id||project.title} className="border border-slate-200 p-6"><h3 className="text-xl font-medium">{project.title}</h3>{project.description&&<p className="mt-3 leading-7 opacity-70">{project.description}</p>}</article>)}</div></section>;}
