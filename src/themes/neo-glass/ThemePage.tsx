/* eslint-disable react-hooks/static-components */
import type { ThemePageProps } from "../types";
export function ThemePage({config,profile}:ThemePageProps){const Panel=({id,title,children,show=true}:{id:string;title:string;children?:React.ReactNode;show?:boolean})=>show?<section id={id} className="mx-auto max-w-6xl px-6 py-16"><div className="rounded-3xl border border-white/15 bg-white/5 p-8 backdrop-blur"><h2 className="text-sm uppercase tracking-[.25em] text-violet-300">{title}</h2><div className="mt-5">{children}</div></div></section>:null;return <main><nav className="mx-auto flex max-w-6xl justify-between px-6 py-7"><a href="#hero">{config.name||profile.username}</a><span className="opacity-50">Explore</span></nav><Panel id="hero" title="Portfolio"><h1 className="text-5xl font-semibold md:text-7xl">{config.headline||config.name||"Interfaces with atmosphere."}</h1><p className="mt-6 max-w-2xl leading-8 opacity-70">{config.about}</p></Panel><Panel id="about" title="About" show={!!config.about}><p className="leading-8 opacity-70">{config.about}</p></Panel><Panel id="skills" title="Skills" show={!!config.skills?.length}><div className="flex flex-wrap gap-3">{config.skills?.map(x=><span key={x.id||x.name} className="rounded-full bg-violet-300/10 px-4 py-2">{x.name}</span>)}</div></Panel><Panel id="projects" title="Projects" show={!!config.projects?.length}>{config.projects?.map(x=><p key={x.id||x.title} className="border-b border-white/10 py-4">{x.title} <span className="opacity-50">{x.description}</span></p>)}</Panel><Panel id="experience" title="Experience" show={!!config.experience?.length}>{config.experience?.map(x=><p key={x.id||x.company}>{x.role} · {x.company}</p>)}</Panel><Panel id="education" title="Education" show={!!config.education?.length}>{config.education?.map(x=><p key={x.id||x.institution}>{x.degree} · {x.institution}</p>)}</Panel><Panel id="certificates" title="Certificates" show={!!config.certificates?.length}>{config.certificates?.map(x=><p key={x.id||x.name}>{x.name}</p>)}</Panel><Panel id="contact" title="Contact"><div className="flex gap-4">{config.githubUrl&&<a href={config.githubUrl}>GitHub</a>}{config.linkedinUrl&&<a href={config.linkedinUrl}>LinkedIn</a>}</div></Panel><footer className="mx-auto max-w-6xl px-6 py-10 opacity-60">{config.name||profile.username}{!profile.isPremium&&<span className="float-right">Built with OrixaAi</span>}</footer></main>;}
